{
  "name": "@hiroiku/dotagents",
  "version": "5.7.0",
  "description": "AI エージェント規則のパッケージマネージャ。module を ~/.dotagents/modules に置き、Claude Code と Codex へ選んで配る",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hiroiku/dotagents.git"
  },
  "bin": {
    "dotagents": "bin/agents-setup",
    "agents-setup": "bin/agents-setup"
  },
  "scripts": {
    "test": "node --test",
    "prepublishOnly": "npm test"
  },
  "files": [
    "bin",
    "modules"
  ],
  "engines": {
    "node": ">=24",
    "bun": ">=1.3"
  }
}
