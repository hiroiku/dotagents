{
  "name": "@hiroiku/dotagents",
  "version": "2.1.0",
  "description": "AI エージェント規則のパッケージマネージャ。modules/ を ~/.claude ・ <project>/.claude へ配る",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hiroiku/dotagents.git"
  },
  "bin": {
    "dotagents": "bin/agents-setup",
    "agents-setup": "bin/agents-setup"
  },
  "scripts": {
    "test": "node --test",
    "prepublishOnly": "npm test"
  },
  "files": [
    "bin",
    "modules"
  ],
  "engines": {
    "node": ">=24",
    "bun": ">=1.3"
  }
}
