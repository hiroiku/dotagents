# collaboration

ユーザーとの対話を担当するセッションと、実作業を担当するサブエージェントを分けるモジュール。[AGENTS.md](AGENTS.md) を常設ルールとして配布する。

この作業分担を使うプロジェクトに `dotagents install collaboration` で追加する。worktree の統合手順は `git` モジュール、完了時の独立レビューは `review` モジュールが持つ。
