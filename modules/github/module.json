{
  "description": "How to put GitHub issues to work: decomposition into sub-issues, milestones that can close, labels a query actually runs, types that span repositories. Install it where the tracker is GitHub.",
  "requires": ["gh"]
}
